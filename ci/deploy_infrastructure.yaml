platform: linux
inputs:
  - name: terraform-repo
run:
  path: bash
  args:
    - -exc
    - |
      cat >$GOOGLE_APPLICATION_CREDENTIALS <<EOL
      $SERVICE_ACCOUNT_JSON
      EOL

      cd terraform-repo

      tfenv use "$(< .terraform-version)"

      ./create.sh
